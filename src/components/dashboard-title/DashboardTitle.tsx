import { ReactNode } from 'react';

const DashboardTitle = ({ children }: { children: ReactNode }) => {
  return <div style={{ fontSize: 24, fontWeight: 600 }}>{children}</div>;
};

export default DashboardTitle;

//s
